[package]
name = "debug-assistant-sdk"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"
description = "Rust SDK for debug-assistant"

[dependencies]
reqwest = { version = "0.12", features = ["json", "blocking"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

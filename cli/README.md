# debug-assistant / cli

```bash
debugger report \
  --project PaperAssistant \
  --module backend \
  --type TimeoutError \
  --message "转换超时" \
  --context '{"stage":"文献综述"}'
```

对应 SPEC：项目一 §四.2 命令行
